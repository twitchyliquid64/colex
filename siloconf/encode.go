package siloconf

import (
	"errors"

	"github.com/twitchyliquid64/colex/controller"
)

// Build converts a configuration struct into an object from which objects
// can be built.
func Build(c *Silo) (*controller.Options, error) {
	return nil, errors.New("not implemented")
}
